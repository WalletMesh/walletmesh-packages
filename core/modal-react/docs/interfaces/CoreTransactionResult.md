[**@walletmesh/modal-react v0.1.0**](../README.md)

***

[@walletmesh/modal-react](../globals.md) / CoreTransactionResult

# Interface: CoreTransactionResult

Defined in: core/modal-core/dist/services/transaction/types.d.ts:453

Complete transaction result with tracking information and helper methods.

Provides comprehensive information about a transaction's lifecycle, from submission
to confirmation. Includes a `wait()` method for awaiting confirmation.

## Remarks

- The `id` is an internal identifier, while `hash` is the blockchain transaction hash
- Status updates as the transaction progresses through its lifecycle
- The `wait()` method provides a convenient way to await confirmation
- Error details are included if the transaction fails at any stage

## Example

```typescript
// Send and track a transaction
const result = await txService.sendTransaction(params);

console.log(`Transaction ID: ${result.id}`);
console.log(`Transaction Hash: ${result.hash}`);
console.log(`Status: ${result.status}`);

// Monitor status
if (result.status === 'broadcasting') {
  console.log('Transaction is being broadcast...');
}

// Wait for confirmation
try {
  const receipt = await result.wait();
  console.log('Transaction confirmed!');
  console.log(`Gas used: ${result.gasUsed}`);
} catch (error) {
  console.error('Transaction failed:', result.error);
}

// Access metadata
if (result.metadata?.description) {
  console.log(`Transaction: ${result.metadata.description}`);
}
```

## Properties

### blockHash?

> `optional` **blockHash**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:523

Hash of the block containing the transaction.
Copied from receipt for convenience.

***

### blockNumber?

> `optional` **blockNumber**: `number`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:518

Block number where transaction was included.
Copied from receipt for convenience.

***

### chainId

> **chainId**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:468

Chain ID where the transaction was sent.
Matches the connected chain at time of sending.

***

### chainType

> **chainType**: [`ChainType`](../enumerations/ChainType.md)

Defined in: core/modal-core/dist/services/transaction/types.d.ts:473

Type of blockchain (evm, solana, aztec).
Determines how the transaction is processed.

***

### effectiveGasPrice?

> `optional` **effectiveGasPrice**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:533

Actual gas price paid (EVM with EIP-1559).
Copied from receipt for convenience.

***

### endTime?

> `optional` **endTime**: `number`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:513

Unix timestamp when transaction completed.
Set when status becomes 'confirmed' or 'failed'.

***

### error?

> `optional` **error**: `object`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:503

Error details if transaction failed.
Includes stage and error message.

#### category

> **category**: `"wallet"` \| `"user"` \| `"network"` \| `"general"` \| `"validation"` \| `"sandbox"`

#### cause?

> `optional` **cause**: `unknown`

#### classification?

> `optional` **classification**: `"network"` \| `"permission"` \| `"provider"` \| `"temporary"` \| `"permanent"` \| `"unknown"`

#### code

> **code**: `string`

#### data?

> `optional` **data**: `Record`\<`string`, `unknown`\>

#### maxRetries?

> `optional` **maxRetries**: `number`

#### message

> **message**: `string`

#### recoveryStrategy?

> `optional` **recoveryStrategy**: `"none"` \| `"retry"` \| `"wait_and_retry"` \| `"manual_action"`

#### retryDelay?

> `optional` **retryDelay**: `number`

***

### from

> **from**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:488

Address that sent the transaction.
The connected wallet's active address.

***

### gasUsed?

> `optional` **gasUsed**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:528

Gas consumed by the transaction (EVM only).
Copied from receipt for convenience.

***

### hash

> **hash**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:463

Blockchain transaction hash/signature.
The actual identifier on the blockchain.

***

### id

> **id**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:458

Internal transaction ID for tracking.
Generated by the service, unique per session.

***

### metadata?

> `optional` **metadata**: `Record`\<`string`, `unknown`\>

Defined in: core/modal-core/dist/services/transaction/types.d.ts:538

Application-specific metadata.
Custom data passed in transaction request.

***

### receipt?

> `optional` **receipt**: `TransactionReceipt`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:498

Transaction receipt after confirmation.
Only available when status is 'confirmed'.

***

### request

> **request**: [`EVMTransactionParams`](EVMTransactionParams.md) \| [`SolanaTransactionParams`](SolanaTransactionParams.md) \| `AztecTransactionParams`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:493

Original transaction request parameters.
Preserves the input for reference.

***

### startTime

> **startTime**: `number`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:508

Unix timestamp when transaction started.
Useful for calculating transaction duration.

***

### status

> **status**: [`TransactionStatus`](../type-aliases/TransactionStatus.md)

Defined in: core/modal-core/dist/services/transaction/types.d.ts:483

Current status of the transaction.
Updates as transaction progresses through lifecycle.

***

### wait()

> **wait**: (`confirmations?`) => `Promise`\<`TransactionReceipt`\>

Defined in: core/modal-core/dist/services/transaction/types.d.ts:547

Wait for transaction confirmation.
Returns a promise that resolves with the receipt when confirmed.

#### Parameters

##### confirmations?

`number`

Number of block confirmations to wait for (default: 1)

#### Returns

`Promise`\<`TransactionReceipt`\>

Promise resolving to the transaction receipt

#### Throws

TransactionError if the transaction fails or times out

***

### walletId

> **walletId**: `string`

Defined in: core/modal-core/dist/services/transaction/types.d.ts:478

ID of the wallet that sent the transaction.
Useful for multi-wallet applications.

/**
 * Global setup for modal-core tests
 */

import { setupModalCoreTestEnvironment } from '../helpers/vitestHelpers.js';

/**
 * Set up the global test environment
 */
export function setupGlobalTestEnvironment() {
  setupModalCoreTestEnvironment();
}
